### Secrets Management & Rotation

- Store secrets in environment or secret managers, never in VCS
- Rotate every 90 days or after incident
- Track owners and expiry in a secrets registry
- Use separate credentials per environment with least-privilege DB roles